#!/usr/bin/env bash
. "$DOTFILES_LIB/clipboard.sh"

if [ -z "$1" ]; then
  echo 'Usage: gpg-copy-key <email>'
else
  gpg --export --armour "$1" | clipboard::copy && \
    echo '=> Public key copied to clipboard.'
fi
